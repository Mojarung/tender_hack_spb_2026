[project]
name = "wb-research"
version = "0.1.0"
description = "Isolated sandbox for Wildberries anti-bot experiments."
requires-python = ">=3.13"
dependencies = [
    "curl_cffi>=0.11",
    "httpx[http2]>=0.28",
    "orjson>=3.10",
]

[tool.uv]
package = false
